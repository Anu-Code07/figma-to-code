{
  "mcpServers": {
    "design2code": {
      "command": "npx",
      "args": ["@design2code/mcp-server"],
      "env": {
        "FIGMA_TOKEN": "${FIGMA_TOKEN}",
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
      }
    }
  }
}
